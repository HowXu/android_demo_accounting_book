<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/black">

    <ImageView
        android:id="@+id/imageView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:scaleType="centerCrop"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.0"
        app:srcCompat="@drawable/intro_pic_k50" />

    <TextView
        android:id="@+id/get_start"
        android:layout_width="135dp"
        android:layout_height="50dp"
        android:layout_marginStart="32dp"
        android:layout_marginBottom="48dp"
        android:background="@drawable/stroke_bg"
        android:fontFamily="@font/vivo"
        android:gravity="center"
        android:text="开始"
        android:textColor="@color/white"
        android:textSize="20sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        tools:ignore="HardcodedText" />

    <TextView
        android:id="@+id/intro_des"
        android:layout_width="236dp"
        android:layout_height="138dp"
        android:layout_marginBottom="32dp"
        android:text="The Best App to manage your money"
        android:textColor="@color/white"
        android:textSize="35sp"
        android:textStyle="bold"
        android:fontFamily="@font/vivo"
        app:layout_constraintBottom_toTopOf="@+id/get_start"
        app:layout_constraintStart_toStartOf="@+id/get_start"
        tools:ignore="HardcodedText,MissingConstraints" />
</androidx.constraintlayout.widget.ConstraintLayout>
    <!--
        scaleType
        用于控制图片如何缩放和显示在其边界内。
            centerCrop
            保持宽高比：图片会保持原始的宽高比例不变
            缩放至完全覆盖视图：图片会被缩放至至少能完全覆盖 ImageView 的宽度或高度
            居中裁剪：如果图片缩放后比视图大，会从中心进行裁剪，确保视图被完全填满
    -->